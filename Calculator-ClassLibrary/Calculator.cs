using System;

namespace Calculator_ClassLibrary
{
    public static class Calculator
    {
        // Method for adding 2 integers
        public static double Add(int lValue, int rValue)
        {
            return lValue + rValue;
        }
    }
}
